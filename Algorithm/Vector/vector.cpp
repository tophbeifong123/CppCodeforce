#include <bits/stdc++.h>
using namespace std;
int main(){
    // defile vector
    vector<int> v;

    // puch vector
    v.push_back(10);
    v.push_back(20);

    // index vector
    cout << v[0] << "\n";
    // size vector
    cout << v.size() << "\n";

    // loop vector
    for(int i:v){
        cout<<i<<"\n";
    }    

    v.erase

    return 0;
}